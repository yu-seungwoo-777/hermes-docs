# 2026-08-24 일일 보고 — dokploy

작성자: dokploy 프로필 (Hermes 워커)
기준: 2026-08-24 00:00~21:00 KST
오늘의 활동은 전부 사용자 직통 세션(20260824_033327_b217c334, "Doc maker prod dev 배포상태 파악", 12:33~16:29 KST)에서 수행. 칸반 태스크로 처리한 건 없음(당일 완료 태스크 0건).

## (a) 변경 조작

1. doc-maker App(mc-AL1az…) 재빌드 — application.deploy (12:59 KST, 사용자 지시 "재빌드할것")
   - 빌드 성공(3분28초, sharp 에러 소멸) → .252 VM CPU 타입 host 변경 적용 확인. 이후 라이브 서비스 최신 코드 반영
2. 프리뷰 배포 활성화 — application.update (13:14 KST, 사용자 지시)
   - isPreviewDeploymentsActive=true, previewWildcard=*.gobongs.com → 이후 사용자 결정으로 null(traefik.me 기본값) 복귀
3. 앱 env 구성 — saveEnvironment + application.update 다수 (13:14~16:26 KST)
   - 메인 env / previewEnv에 InsForge prod·dev 키 세트 주입 (NEXT_PUBLIC_INSFORGE_{URL,ANON_KEY} + INSFORGE_{PROD,DEV}_{BASE_URL,API_KEY})
   - 최종 상태: 메인 env=**prod InsForge**(nip.io http), previewEnv=dev InsForge — 16:26 KST 사용자 결정("env만 prod로 전환, 빈 DB로 시작")으로 확정
4. CF DNS — gobongs.com 와일드카드 레코드 임시 A(192.168.0.252) 전환 후 CNAME(whatumong.iptime.org, DNS only) 원복 (13:37~13:43 KST, 사용자 지시 "프리뷰는 traefik 기본값")
5. 외부 공개 개통 — doc-maker.vector.ai.kr (13:43 KST, 사용자 지시)
   - domain.create(첫 시도 https:true+letsencrypt → 308 루프 → domain.update로 https:false+none 수정) + 재배포
   - CF DNS CNAME doc-maker → 터널(8ab210a2….cfargotunnel.com, proxied) 생성
   - CF 터널 ingress에 doc-maker.vector.ai.kr → http://dokploy-traefik:80 추가
   - E2E 검증 200("doc-maker — 견적 작성")
6. 내부 도메인 제거 — docmaker-app.gobongs.com domain.delete + 재배포 (13:58 KST, 사용자 지시) → 404 확인, 외부 도메인만 유지
7. 장애 대응 2건의 env 롤백/재지정 (아래 (c) 참조) — 배포 총 6회, env 교체 총 4회

## (b) 읽기 전용 조사·모니터링

- doc-maker 배포상태 전수 조사: project.all / compose.one / deployment.allByType — InsForge prod·dev compose는 정상(done), doc-maker App만 최근 2회 빌드 error
- sharp 빌드 실패 원인 분석: 실패 로그("Unsupported CPU: Prebuilt binaries require v2 microarchitecture") vs 성공 로그(sharp 0회 언급) 대조, .253(같은 호스트 N100)에서 sharp 정상 로드 실측 → .252 VM CPU 타입 kvm64/qemu64 추정, 사용자가 Proxmox에서 host로 변경 후 빌드 성공으로 확정
- Vercel Preview Deployments 기능 조사(web) + Dokploy 동일 기능(Preview Deployments) 문서 확인 — 사용자 질문 답변용
- PR #45 더미 PR로 프리뷰 E2E 검증: 자동 생성·빌드 done 확인, LAN에서 .252 직결 200. 와일드카드 DNS가 WAN(iptime DDNS)으로 풀려 NAT 루프백 불가 실측 → traefik.me 기본값 확정의 근거
- 터널 308 루프 원인 분석: umami(https:false, traefik:80 직접 200)와 실측 비교 → CF 터널 공개 시 Dokploy 도메인은 https:false+none 필수 패턴 확정
- InsForge prod/dev DB 상세 파악: prod=테이블 0개(빈 DB)+유저 1명(사용자가 오늘 가입), dev=스키마 6테이블+견적 19건·거래처 7곳+유저 16명(실계정 whatumong@naver.com 포함)
- prod 전환 전 사전 검증: quotes→quote_items 임베디드 쿼리 prod에서 200 확인 후 전환

## (c) 실패·재시도

1. doc-maker App 자동배포 빌드 실패 2회 (09:35, 10:50 KST — 세션 시작 전 push 트리거)
   - 원인: 새 라우트 /api/share/[token]/render의 sharp가 .252 VM CPU(x86-64-v2 미달)에서 프리빌트 로드 불가
   - 해결: 사용자가 .252 CPU 타입 host 변경 → 12:59 재빌드 성공. 롤백 불필요(구버전 계속 서빙 중이었음)
2. 외부 공개 첫 시도 308 무한 리다이렉트 (13:43 KST)
   - 원인: https:true+letsencrypt로 등록하니 traefik:80 라우터가 https로 리다이렉트, 터널은 80으로만 다이얼 → 루프
   - 해결: domain.update로 https:false+none → 재배포 후 200. 나의 설정 선택 오류(스킬의 umami 패턴이 정답이었음)
3. 로그인 "Network request failed: fetch failed" (15:57 KST 사용자 보고)
   - 원인: 내가 env URL을 https://docmaker.gobongs.com(자체 CA)으로 교체한 것 — 앱 컨테이너가 내부 CA 미신뢰 → 서버사이드 fetch TLS 실패. 나의 변경이 유발
   - 해결: http nip.io 원복 + 재배포 → 로그인 체인 401 정상 패스스루 확인
4. 로그인 후 견적 목록 500 (React #441, 16:06 KST 사용자 보고)
   - 원인: 메인 env가 prod InsForge(빈 DB)를 가리킴 → 'quotes↔quote_items 관계 없음'. 역시 env 전환에서 비롯
   - 해결: dev 바인딩으로 복원해 서비스 정상화 → 데이터 현황 보고 후 사용자 결정으로 최종 prod 전환(빈 DB로 시작) 확정. dev 데이터는 dev 인스턴스에 무손실 보존
5. 프리뷰 와일드카드 DNS NAT 루프백 (13:33 KST 발견)
   - *.gobongs.com이 WAN DDNS로 풀려 LAN 브라우저에서 프리뷰 URL 미개통 → 사용자 결정: 프리뷰는 traefik.me 기본값, 와일드카드는 DDNS 원복

## (d) 진행 중/보류와 다음 계획

- doc-maker 앱 = prod InsForge(빈 DB) 바인딩 라이브. 견적 데이터가 필요해지면 dev→prod 선택적 이관(6테이블) 가능 — 요청 시 진행
- CF DNS에 docmaker-app.gobongs.com A레코드 잔존(traefik 404, 무해) — 삭제 원하면 처리 가능
- traefik.me 프리뷰 도메인의 LAN 접근성(127.0.0.1 해석 특성상 ssh 터널/hosts 필요 가능) — PR 실사용 시점에 확인 예정
- 기존 과제 유지: Dokploy UI 등록 CF 토큰의 DNS 전용 최소권한 재발급
- 자동배포·프리뷰 파이프라인 정상 가동 상태 — 특별한 모니터링 이슈 없음

비고: 오늘 밝혀진 운영 지식 3건(터널 패턴 https:false 필수, saveEnvironment이 previewEnv 무시 버그, .252 CPU타입 host 전환)은 메모리에 반영 완료.
