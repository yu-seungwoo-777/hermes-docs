# 2026-08-21 일일 보고 — dokploy

> 작성: 2026-08-22 소급 작성 (칸반 기록 기반 복원 — 보고 문서 제도 시행 전날분)

## 변경 조작 (즉시 대장 보고 대상)

### 1. InsForge doc-maker 인스턴스 prod/dev 2벌 신규 배포
- Dokploy 신규 프로젝트 `doc-maker` (production / development 환경 2개)
- prod: `http://insforge-docmaker.192.168.0.252.nip.io` (composeId j5O4F7P7_KUd678M0hqEP)
- dev: `http://insforge-docmaker-dev.192.168.0.252.nip.io` (composeId tqbIObgAxmmL1at28SI07)
- 양쪽 배포 done + `/api/health` 200 (v2.3.1) + CRUD 스모크 통과
- 시크릿: `secrets/doc-maker-{prod,dev}.env` (권한 600, 독립 생성)

### 2. Mac Studio ↔ Hermes 양방향 SSH 설정
- Hermes→Mac: `ssh MAC-STUDIO` alias (192.168.0.15, yu-seungwoo)
- 사용자 요청으로 Mac의 doc-maker dev 환경에 InsForge dev/prod 접속 정보 `.env.local` 주입 (600, gitignore 확인)
- Mac에서 양 인스턴스 health + API 키 인증 200 검증 완료

## 운영 지식 업데이트 (스킬 반영 완료)
- `compose.create`의 composeType은 `"docker-compose"`|`"stack"`만 허용 (400 조용한 실패 함정)
- InsForge: 토큰 필드 `accessToken`, 테이블 생성 `tableName` 필드, PostgREST 캐시 지연 404
- InsForge 사용자 계정: admin 비활성화 불가(env 필수), 유저 생성/삭제 API 동작 (앱 Auth용)
- API 키는 인스턴스당 1개 고정·회전 불가·admin급 권한 — 유출 시 재구축 필요

## 조사·모니터링 (읽기 전용)
- 칸반 끝단 테스트 (t_a54db460, 09:10 완료): Dokploy `project.all` 조회 — 프로젝트 3개(umami·insforge-test·edge, 모두 done), 앱 1 / compose 2 / 매니지드 DB 0. 시크릿 출력·변경 조작 0건.

## 대장 보고
- `[dokploy→default]` 보고 태스크 t_7ef44f3f 제출 (18:17) → 대장 검증 완료: "보고 내용 전량 사실로 확정"

## 진행 중 / 보류
- Dokploy 네이티브 삭제 보호 기능 없음 → 백업/감시 cron 제안 상태 (**사용자 승인 대기**)

## 참고
- 삭제·위험 조작 없음 (read + create + env 주입만)
- 시크릿 평문 노출 0건
