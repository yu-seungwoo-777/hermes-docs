# 2026-09-02 일일 보고 — dokploy (작성: dokploy 워커, 2026-09-02 21:10 KST 기준)

## (a) 변경 조작

- 없음 (0건). 패널 webserver host=dokploy-server-777.gobongs.com, updatedAt=2026-08-25T06:08 UTC 유지 — 금일 저장 조작 없음.
- 서비스 구성 변경(도메인·env·배포 트리거) 없음. 칸반 금일 태스크는 일일보고 본건(t_12b13c35) 1건뿐.

## (b) 읽기 전용 조사·모니터링

**전체 현황 (project.all)**: 프로젝트 3개(doc-maker, umami, edge), 서비스 6개 전부 done.
- doc-maker: App(앱, done) + insforge compose 2개(prod/dev, done)
- umami: compose done
- edge: cloudflared(앱, done) + tailscale compose done

**배포 이력**: 금일 배포 0건 (App·compose 4개 전체 금일 카운트 0). 최종 배포는 08-29 16:23 KST doc-maker App PR#66 (done) — push 무인 파이프라인 4일째 안정 (신규 push 없음).

**구성 무결성 점검**
- insforge prod/dev compose: env 키 8개 양쪽 동일(JWT_SECRET, ENCRYPTION_KEY, POSTGRES_PASSWORD, ROOT_ADMIN_*, API_BASE_URL, VITE_API_BASE_URL, INSFORGE_TELEMETRY_DISABLED) — 08-24 확정 구성 유지.
- compose 등록 도메인: prod=insforge-docmaker.nip.io / dev=insforge-docmaker-dev.nip.io (포트 7130, https:false) — 변동 없음.

**외부 엔드포인트 (브라우저 UA 검증)**
- https://dokploy-server-777.gobongs.com/ → 200
- https://umami-server-777.gobongs.com/ → 200
- https://analysis.vector.ai.kr/ → 200
- https://doc-maker.vector.ai.kr/ → 307 → sign-in 200 (인증 리다이렉트 정상)
- 웹훅 POST /api/deploy/github → 401 (도달+서명검증 정상)
- insforge prod /api/health → 200, dev /api/health → 200 (v2.3.1)
  ※ 참고: 헬스 체크 시 insforge-docmaker-prod.nip.io(미등록 호스트)는 404 — 등록 도메인은 insforge-docmaker.nip.io가 정답. 과거 문서 중 prod 호스트에 -prod 접미사를 쓴 표기가 있으면 정정 대상.
- umami 내부 http(umami.nip.io) → 200

**잔존 이슈(신규 아님, 08-31 보고에서 계속)**
- docmaker-server-777.gobongs.com: HTTPS 접속 시 TLS unrecognized name (SNI alert 112) — Dokploy 미등록 호스트로 traefik 라우터 없음. 미사용 주소로 보이며 CF DNS 정리 후보.
- 패널 DNS Providers 토큰 최소권한(DNS 전용) 재발급 과제 — 미착수 상태 유지.

## (c) 실패·재시도

- 서비스 장애·배포 실패 0건.
- 운영 참고(환경 특성, 재확인): 단일쿼리 모드에서 `python3 -c`/heredoc 실행이 승인 정책으로 차단 — 워크스페이스 스크립트 파일 실행으로 우회(금일도 동일 처방, 영향 없음). execute_code도 동일 정책으로 사용 불가.

## (d) 진행 중·보류와 다음 계획

- 진행 중/보류 작업: 없음. 사용자 직접 요청도 금일 0건 (프로필 세션 DB 기준 당일 kanban 세션 1건=본 보고뿐).
- 다음 계획 (사용자 결정 필요):
  1. docmaker-server-777.gobongs.com 처리 — 미사용 확정 시 CF DNS 레코드 삭제 권고 (삭제는 사용자 확인 후).
  2. Dokploy UI DNS Providers 토큰을 DNS 전용 최소권한으로 재발급 (기존 과제).
  3. (소규모) 문서 내 insforge prod nip.io 호스트 표기 정정 — -prod 접미사 표기는 실제 등록 도메인과 불일치.
