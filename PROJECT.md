# mailto 캠페인 프로젝트 — 조사 결과 및 작업 지시서

> 이 문서는 세션이 바뀌어도 작업을 이어갈 수 있게 모든 조사 결과와 다음 작업을 담은 기준 문서다.
> 최종 갱신: 2026-09-12

## 1. 프로젝트 개요

**목적**: 다수의 참여자가 각자의 메일 계정으로 특정 기관(들)에 항의 메일을 보내는 캠페인 웹페이지 제작. 수신 측 필터에 의한 일괄 스팸 분류를 피해야 목적 달성이 가능하다.

**결정된 설계 원칙**:
- 참여자 1명당 기관 1곳 = 1통 (to/cc에 여러 기관 동시 발송 금지 — 스팸 점수 직격 + 캠페인 신뢰도 하락)
- 기관마다 내용(제목·본문)이 달라야 함 — 해시 매칭 기반 일괄 스팸 분류 회피
- 폴백은 클립보드만 (웹메일 딥링크는 실서비스에 미포함 결정)

## 2. mailto 동작 점검 조사 결과 (2026-09-12 실측 완료)

테스트 페이지(GitHub Pages, HTTPS)로 4개 기기 + 인앱 브라우저에서 검증.

### 기기별 결과

| 테스트 | iPhone Safari (iOS 18.7) | Android 삼성브라우저 30 | macOS Safari 26.6 | macOS Chrome 153 |
|---|---|---|---|---|
| T1 mailto 기본 | 됨 | 됨 (Gmail앱 연결) | 됨 | 됨 |
| T2 주소+제목+본문 %0D%0A | 됨 | 됨 | 됨 | 됨 |
| T3 본문 %0A만 | 됨 | 됨 | 됨 | 됨 |
| T4 bcc 포함 | 됨 | 됨 | 됨 | 됨 |
| 본문 길이 500~4000자 | 됨 | 됨 | 됨 | 됨 |
| Gmail 웹 컴포즈 | 안됨 | 안됨 | 됨 | 됨 |
| Outlook 웹 컴포즈 | 안됨 | 안됨 | 됨 | 됨 |
| 네이버 메일 | 안됨 | 안됨 | 안됨 | 안됨 |
| 클립보드 writeText | 됨 | 됨 | 됨 | 됨 |
| 클립보드 ClipboardItem (HTML+이미지) | 됨 | 됨 | 됨 | 됨 |
| **인앱 브라우저** (텔레그램, 카톡) | **됨** | **됨** | - | - |

### 핵심 결론
1. **mailto: 전 환경(모바일/데스크톱/인앱) 완전동작** — 제목·복수줄 본문·bcc·4000자·줄바꿈 두 방식(%0A, %0D%0A) 모두 손실 없음. 별도 폴백 없이 mailto 단독 사용 가능
2. **웹메일 딥링크는 데스크톱(로그인 상태)에서만 동작** — 모바일은 앱 리다이렉트/로그인 문제로 실패. 실서비스 폴백에서 제외하기로 결정
3. **네이버 메일: 전 기기 실패** — `mail.naver.com/v2/folders/0/write?to=` 형태는 비공식 추정 경로, 파라미터 미반영. 네이버는 웹 작성 화면 외부 파라미터 주입 미지원으로 판단
4. **클립보드(텍스트/HTML+이미지) 전 환경 지원** — 필요시 폴백으로 사용 가능하나, mailto가 전 환경 동작하므로 현재 설계에서는 미포함
5. 줄바꿈은 `%0D%0A` 인코딩으로 통일 (양쪽 방식 모두 되었으나 표준 준수)
6. iOS Safari 클립보드·mailto는 클릭 핸들러 안에서 동기 시작 필수 (`window.location.href` 직접 할당)

### 다중 수신자 처리 (RFC 6068)
- 쉼표(,) 구분 = 표준. 세미콜론(;)은 Outlook 데스크톱 전용
- **그러나 캠페인 설계상 to/cc 다수 수신자는 사용하지 않기로 결정** (위 설계 원칙 참조)

## 3. 배포 환경 (GitHub Pages)

- **저장소**: `yu-seungwoo-777/hermes-docs` (2026-09-12 public 전환 + Pages 활성화, source: main 브랜치 루트)
- **URL**: https://yu-seungwoo-777.github.io/hermes-docs/
- **인증서**: Let's Encrypt (*.github.io) — 공인 인증서 확인 완료
- **작업 디렉토리(이 서버)**: `/tmp/hermes-docs` (git clone) — 세션 재시작 시 소실 가능. 재생성: `gh repo clone yu-seungwoo-777/hermes-docs /tmp/hermes-docs`
- **로컬 원본(이 서버)**: `/root/protest-page/index.html` (참여 페이지 최신본)

### GitHub 토큰 제약 (중요)
- `gh auth` 토큰은 **fine-grained PAT**: repo 생성·공개전환은 가능하나 **Contents:write는 hermes-docs 저장소에만 부여됨**
- 신규 repo 생성 후 push 시도 → 403. **push가 필요하면 반드시 hermes-docs repo 사용**
- Pages 설정: legacy 방식(main 브랜치 루트 서빙), https_enforced: true

### 재배포 절차
```bash
gh repo clone yu-seungwoo-777/hermes-docs /tmp/hermes-docs   # 세션 새로 시작 시
cp <새index.html 경로> /tmp/hermes-docs/index.html
cd /tmp/hermes-docs
git add index.html && git commit -m "update page" && git push origin HEAD
# 반영까지 ~30-60초 대기 후 curl로 확인
```

## 4. 현재 구현 상태 (테스트 버전 배포됨)

`/root/protest-page/index.html` — 현재 Pages에 배포된 테스트 페이지:

- **입력**: 이름 + 한마디 → `참여하겠습니다` 버튼
- **수신처 리스트** 표시 → 기관별 `메일 쓰기` 버튼 → mailto 오픈
- **기관마다 템플릿 다각화**: 제목 풀 5종 × 본문 프레임 5종 무작위 조합, 이름·한마디가 `{name}`/`{msg}`/`{msg-s}` 자리표시자로 삽입
- 예시 수신처: 기관 A/B/C (더미)
- `{issue}` 자리: "이번 사안" 더미 텍스트

### 실서비스 전환 시 교체 항목 (파일 상단 스크립트 내 주석 표시됨)
1. `DESTS` 배열 — 실제 기관명·이메일 주소
2. `BODY_FRAMES` 배열 — 실제 항의 내용 기반 프레임 5종 이상 (문장 구조가 서로 다르게)
3. `SUBJ_POOL` 배열 — 실제 제목 변형 5종 이상
4. `ISSUE` 상수 — 항의 이슈 한 줄

## 5. 다음 작업 (세션 이어가기)

### 대기 중인 사용자 입력
- [ ] 실제 항의 대상 기관명 + 이메일 주소 목록
- [ ] 항의 내용/요구사항 원문

### 이후 작업 순서
1. 사용자로부터 위 2가지 수령
2. `SUBJ_POOL`/`BODY_FRAMES`를 실제 내용으로 작성 (변형 다양성 확보: 문장 구조·어휘·길이가 실제로 달라야 함, 단순 단어 치환은 해시 우회 불가)
3. `DESTS` 교체, `ISSUE` 설정
4. 로컬 테스트 → `/tmp/hermes-docs`에 push → curl 200 확인
5. 사용자에게 URL 전달, 실기기 최종 확인 요청 (입력→버튼→메일앱 오픈 흐름)

### 참고: 스팸 필터 회피 설계 원칙 (이미 구현됨)
- 기관별 다른 템플릿 (프레임 구조 자체가 다름)
- 참여자 이름·한마디 삽입으로 개인화
- 안내문에 "자기 경험 한 줄 추가" 유도 (참여자가 직접 쓴 문장은 어떤 필터로도 유일문자)
- "며칠에 걸쳐 나눠 보내기" 유도 (시간 분산)
- 링크·첨부 최소화

## 6. 관련 세션 맥락

- mailto 동작 점검 페이지 최초 제작·검증: 2026-09-12 (이 세션)
- 인앱 브라우저 동작 확인은 사용자 직접 테스트 (텔레그램·카톡에서 성공)
- 스팸 회피 방법 문의 → "수신자 동의 기반 정상 캠페인"으로 방향 확정 후 설계 진행
- 사용자 승인 하에 hermes-docs repo를 public으로 전환함 (원래 private) — 사용자가 문제 삼으면 private 복귀 + Dokploy+CF터널 대안 경로로 이전 필요
